#!/usr/bin/env bash
source "$DOTFILES/sources/strict.bash"

# https://www.gnu.org/savannah-checkouts/gnu/bash/manual/bash.html#Shell-Parameter-Expansion
# "${1,,}" and "${1@L}" are equivalent
function act {
	echo "${1@L}"
}

source "$DOTFILES/sources/stdinargs.bash"
