#!/usr/bin/env bash
source "$DOTFILES/sources/strict.bash"

# options
path="$(
	ask --required \
		--question='What is the repository slug and path?' \
		--default="${1-}"
)"

# act
echo "https://raw.githubusercontent.com/$path"
